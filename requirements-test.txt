# Test dependencies — intentionally excludes chromadb, sentence-transformers,
# and pdfplumber. Those are stubbed via sys.modules in conftest.py so tests
# run without heavy downloads.
pytest==8.2.0
httpx==0.27.0
fastapi==0.115.6
pydantic==2.10.4
python-jose[cryptography]==3.3.0
python-dotenv==1.0.1
python-multipart==0.0.20
requests==2.32.3
